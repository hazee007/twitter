import React from 'react';
import { TweetButton } from './button.styles';

const TwitterButton = ({ children }) => {
  return <TweetButton> {children}</TweetButton>;
};

export default TwitterButton;
